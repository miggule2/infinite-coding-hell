### [boj] / 1991_트리순회 / 실버1 / 30분

- 전위순회, 중위순회, 후위순회를 구현하는 문제
- 입력을 받아 tree 딕셔너리를 만들어 사용.
- 방문 순서에 따라 노드를 먼저 방문할지, 왼쪽 자식노드를 먼저 방문할지, 오른쪽 자식노드를 먼저 방문할지에 따라 각각의 함수 구현
- result.append()를 해주는 위치가 달라짐

### [programmers] / 석유시추 / LV2 / 2시간

- 그래프 탐색을 사용하는 문제로 col로 내려갔을때 석유를 만나면 탐색시작
- bfs를 이용해서 연결된 석유그룹을 찾아서 dic에 넣어 각각의 chunk_id를 부여
- 모든 칸을 for문으로 돌면서 방문기록이 없고 값이 1인(석유가 있는)곳에 대해 탐색을 이어나간다.
- set을 이용하여 중복제거를 하고 열 기준으로 돌면서 최대 석유량을 계산

### [boj] / 2178_미로탐색 / 실버1 / 1시간

- 이전에 풀었던 문제중 석유시추와 비슷한 느낌의 문제로 다시 풀어봄
- 석유시추와는 다르게 방문기록을 visited로 따로 관리하지 않고 탐색을 이어나가면서 값이 1인 칸에서 값을 더해나가는 식으로 하여 사용함.
- 위 방법을 통해 방문기록과 총 이동 거리를 한번에 관리 가능함
- 석유시추와는 달리 시작 좌표부터 출발하여 방향을 탐색(for문 range(4))를 사여 1인 곳으로만 가도록 구현함.